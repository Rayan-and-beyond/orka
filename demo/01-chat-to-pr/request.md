Add a GET /healthz endpoint to https://github.com/sozercan/orka-demo-inventory
that returns HTTP 200 with the JSON body {"status":"ok"}. Cover it with a unit
test in the existing style and add it to the API table in README.md. Open a
pull request against main.
